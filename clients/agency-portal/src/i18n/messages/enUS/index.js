const app = {
  'app.title.full': 'Leg Day Collections Software',
  'app.title.short': 'Leg Day'
}

const state = {
  'state.example.initialState.text': 'State is connected.'
}

const enUS = {
  ...app,
  ...state
}

export default enUS
