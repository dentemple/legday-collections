import PropTypes from 'prop-types'

export const User = {
  createdAt: PropTypes.number,
  displayName: PropTypes.string,
  headerUrl: PropTypes.string,
  profileUrl: PropTypes.string
}

const propTypes = { User }

export default propTypes
